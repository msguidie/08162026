====================================================================================================
chlligence/PPO-splendorReinforcementLearning | relevance 8 | license NONE. No LICENSE/COPYING file (I searched the whole tree case-insensitively), no SPDX headers, no license section in README.md. Under default copyright the code is All Rights Reserved — you have no grant to copy, modify, or redistribute it. game-data/cards_data.xlsx also has no stated provenance. Treat everything here as read-only reference: derive designs and algorithms from it (ideas are not copyrightable) but write your own implementation rather than lifting files, and if you want any literal code, ask the author for an explicit license first.
-- algorithm: MaskablePPO from sb3-contrib 2.3.0 (SB3 2.3.0, gymnasium 0.29.1, torch 2.11+cu128, Python 3.10.11 on Windows). No MCTS, no search, no minimax, no distillation — grepped for mcts/monte-carlo/minimax/puct: zero hits.  Hyperparameters (/home/user/chlligence/ppo-splendorreinforcementlearning/training/config.py):   n_steps=1024 per env x 22 envs = 22,528-step rollout; batch_size=256 (88 minibatches x 4 epochs = 352 grad steps/update, ~22 updates per 500k-step generation)   n_epochs=4; learning_rate=5e-5 cosine-annealed to 1e-5 over 100 generations (self_play_loop.py:95-108, final checkpoint shows lr=1.0009868792685368e-05)   gamma=0.99; gae_lambda=0.92; clip_range=0.15; vf_coef=0.75; max_grad_nor
-- rules_fidelity: 2-player ONLY, no nobles at all. Hardcoded throughout: `players: Tuple[PlayerState, PlayerState]`, `1 - player_idx` as "the opponent", `turn_number % 2` for final-round parity, `_determine_winner` comparing exactly two players.  Implemented faithfully (and matching OUR rules): - 4 face-up per tier, replacement drawn from deck immediately after buy or reserve (rules.py:105-153) - Max 3 reserved; reserve grants 1 gold if gold available (rules.py:128-136) - Take 2 of same colour requires the stack to have >= 4 BEFORE taking (action_mask.py:44) — identical to our rule, and with only 4 per colour at 2p it is only legal from a full stack - Payment: colour tokens first, then gold covers the remaind
-- action_space: Discrete(51), flat, with a boolean legality mask supplied both via info["action_mask"] and via the `action_masks()` method that sb3-contrib calls through env_method (splendor/action_mask.py, N_ACTIONS=51):     0-9   take 3 different gems — the 10 combos of C(5,3), hardcoded as COMBO_3_DIFFERENT in constants.py:41-46. Legal iff all three colours have >= 1.   10-14  take 2 of the same colour. Legal iff that colour has >= 4 available.   15-26  reserve face-up card at flat position 0-11 (level = pos//4 + 1, slot = pos%4). Legal iff < 3 reserved and the slot is non-empty.   27-29  reserve blind from the top of deck L1/L2/L3. Legal iff < 3 reserved and the deck is non-empty.   30-41  buy face-up c
-- network: Framework: PyTorch 2.11+cu128 under SB3 2.3.0 / sb3-contrib 2.3.0, MaskableActorCriticPolicy (confirmed by decoding checkpoints/agent_latest.zip's `data` blob).  Custom extractor, /home/user/chlligence/ppo-splendorreinforcementlearning/training/feature_extractor.py:   203 -> Linear(512) -> LayerNorm -> ReLU       -> Linear(512) -> LayerNorm -> ReLU       -> Linear(512) -> LayerNorm -> ReLU       -> Linear(256) -> ReLU        [features_dim = 256]   Orthogonal init, gain sqrt(2), biases zeroed. ~764,160 params.  Heads (net_arch from training/config.py NETWORK_CONFIG, confirmed in the checkpoint's policy_kwargs):   pi: 256 -> 512 -> 512 -> 51 logits   vf: 256 -> 512 -> 512 -> 1   Feature extrac
-- self_play: Opponent lives INSIDE the environment. training/self_play_env.py wraps SplendorEnv; the agent's step() executes the agent's action, then auto-plays opponent plies until it is the agent's turn again or the game ends. Only agent transitions reach the PPO rollout buffer; opponent transitions are discarded.  Opponent pool (training/opponent_pool.py, max_size=30). sample() is a cumulative-threshold cascade:    5%  return None -> uniform-random-legal-action opponent (robustness insurance)   50%  latest entry (by max generation, never by list position — opponent_pool.py:145-154)   10%  uniform random from the pool   ~35% ELO-softmax with temperature 50.0 (the comment at opponent_pool.py:128-131 exp
-- distributed: NO distributed training of any kind. Single process, single GPU (PPO_CONFIG["device"]="cuda"). Grepped for DDP / DistributedDataParallel / DataParallel / torchrun / accelerate / horovod / AMP / autocast / torch.compile / numba / cython / jit: zero hits anywhere in the repo.  Parallelism is entirely CPU env-level: stable_baselines3.common.vec_env.SubprocVecEnv with n_envs=22, sized "~1 per physical core; 24-core Arrow Lake, no HT, sweet spot 22-24" (config.py:49-50). Persistent across all generations.  Thread-oversubscription control, the one genuinely portable performance trick here: - scripts/train.py:23-25 sets OMP_NUM_THREADS / MKL_NUM_THREADS / OPENBLAS_NUM_THREADS to "1" via os.environ.
-- reported_strength: The README makes NO numeric strength claim — no Elo, no win rate vs humans, no baseline comparison. So there is nothing in the prose to be skeptical of; the skepticism belongs to the artifact that IS shipped.  checkpoints/pool_index.json contains the real record for generations 63-99 (the pool is capped at 30, so earlier gens were evicted):   gen 63: elo 1224.69, win_rate_vs_prev 0.575   gen 72-79: elo 1226.8 -> 1229.2, win rates 0.6375, 0.525, 0.475, 0.5625, 0.5625, 0.5375, 0.4625, 0.5   gen 80 (full_eval): elo 1234.03, win rate 0.51   gen 83: 0.3625   gen 86: 0.3875   gen 87: 0.4125   gen 91: 0.375   gen 90 (full_eval): elo 1231.71, win rate 0.46   gen 99: elo 1232.01, win_rate_vs_prev 0.4
-- reuse_for_us: DIRECTLY APPLICABLE (the card economy is nearly ours): - game-data/cards_data.xlsx is 15/18 templates identical to our spec, with an identical schema (Level | Bonus | Point | Black | White | Red | Blue | Green). Swap in our exact template list and splendor/card.py::load_cards + the Card dataclass work unchanged. Same tier sizes 40/30/20, same 5x rotation structure, same even bonus distribution, same max single cost 7. - constants.py's 2-player economy IS ours: 4 tokens per colour, 5 gold, 10-token cap, 3 reserves, 4 face-up per tier, 15 points to trigger, MAX_COST_NORM=7. - rules.py::can_afford (gold as 1:1 wildcard against summed per-colour deficits after bonus) and _purchase_card (colour t
-- ideas_to_steal:
   * Terminal-reward substitution when the OPPONENT's move ends the game (training/self_play_env.py:236-246). The agent's last recorded reward is stale dense shaping from before the opponent moved; the wrapper overwrites it with -opp_reward because rewardcc's terminal is +/-10 zero-sum and Phi is strictl
   * Opponent hot-swap over persistent workers (training/self_play_env.py:170-195 + training/self_play_loop.py:229-244). SubprocVecEnv workers are created once for the whole 100-generation run; each generation the parent does torch.save(model.policy) into a BytesIO and pushes bytes via vec_env.env_method
   * Thread-oversubscription pinning done in the right place (scripts/train.py:23-25 sets OMP/MKL/OPENBLAS_NUM_THREADS=1 via os.environ.setdefault BEFORE any other import so spawned children inherit it; training/self_play_env.py:331 additionally calls torch.set_num_threads(1) inside each worker's _init).
   * Relative zero-sum potential with FROZEN per-call weights (splendor/rewardcc.py:325-345). Phi(s) = Strength(self) - Strength(opp), and both the 5-dim `demand` vector and the (w_point, w_bonus) pair are computed once from prev_state and reused for Phi(prev) and Phi(curr). This kills two spurious rewar
   * Board-demand colour vector (splendor/rewardcc.py:160-178): take face-up T2/T3 cards whose cost-performance >= 0.5, accumulate cost[c] * points_eff per colour, L1-normalise. Then bonus_alignment = dot(demand, player.bonuses). A cheap, interpretable 'is my engine pointed at the cards that actually mat
   * Phase-dependent shaping weights (splendor/rewardcc.py:82-89 and 149-154): w_point rises 0.05 -> 0.15 and w_bonus falls 0.15 -> 0.03, linear in n = cards purchased, saturating at N_SAT=15. Encodes 'engine early, points late' as a smooth schedule keyed on the agent's own progress rather than on wall-c
   * Denial/blocking shaping with a precise trigger (splendor/rewardcc.py:253-319). Reward buying or reserving a card only when the OPPONENT is exactly one gem short of EXACTLY ONE good card, with the total post-gold deficit == 1 and that last gem landing on the card's max-cost colour. The 'exactly one' 
   * Paired-seed side-swapped evaluation (training/evaluate.py:51-137). Both games of a match pair use the SAME seed, so the deck is byte-identical and only the seat assignment differs. This cancels deck-draw variance instead of stacking two independent noisy samples. Also tracks a_wins_as_p0 vs a_wins_a
   * Seat/turn-order decoupling (training/self_play_loop.py:197-209): agent_player_idx = i % 2 across the vec envs while starting_player stays 0 in every env. The agent trains as both first and second player without ever generating the OOD 'second seat moves first' distribution, and any absolute-seat obs
   * Observation-distribution version gate (training/opponent_pool.py:174-245). pool_index.json carries format_version; loading a pre-fix v1 bare-list index raises a RuntimeError that names the incompatibility ('trained under always-P0, turn-flag=0') and tells you to delete checkpoints/. Cheap insurance 
   * Eval provenance tagging (training/evaluate.py:206-279 + opponent_pool.py:27-29). Every pool entry stores elo_source and win_rate_source in {full_eval, incremental_vs_latest, baseline}, so a cheap 80-game estimate can never later be mistaken for a calibrated measurement, and a placeholder is explicit
   * Two-tier eval cadence: a cheap 40-pair check vs the latest opponent EVERY generation with a small K (16), plus a full calibrated eval vs the top-2 pool entries every 10 generations with K=32 that overwrites the cheap estimate (training/evaluate.py:242-273). Keeps a per-generation signal without payi
-- pitfalls:
   * The PBRS policy-invariance claim is not what the code implements. env.py:185 computes gamma*Phi(s') - Phi(s) where s' is the state right after the AGENT's move but BEFORE the opponent replies, while the next agent step measures Phi(s) after the reply. The sum therefore does not telescope; the uncred
   * Auto-discard on token overflow (rules.py:228-263) instead of a discard decision. Their own docstring concedes that 'take only 2 to avoid discarding' is a real skill the agent can never learn. For US this whole subsystem is not merely suboptimal but WRONG: our rule blocks the selection so overflow ca
   * The action space has no take-2-DIFFERENT. Only take-3-different (10), take-2-same (5), take-1 (5). In real Splendor and in our variant, taking exactly 2 different colours is legal and sometimes forced by supply or by the cap. Adding it is +10 actions and touches the mask, the reward's hoarding logic
   * Terminal reward short-circuits the rest of the function (rewardcc.py:349-359 returns early), so the winning move receives no BUILD_BONUS and no STEP_PENALTY, and APPLY_TERMINAL_SHAPING adds -Phi(prev) which can be worth roughly +/-2 to 3 against a +/-10 terminal. Whether that is intended is unclear;
   * BUILD_BONUS (+0.20 per purchased card with cost-performance >= 0.5) sits outside the potential-based skeleton by design, and is per-CARD. Buying many cheap high-CP tier-1 cards farms it. Combined with STEP_PENALTY of only -0.005 and a 200-ply truncation that still pays the full +/-10 outcome, a 'buy
   * observation.py:92 encodes float(state.current_player) — the ABSOLUTE seat index — while every other feature is perspective-normalised. It only works because training alternates seats, and getting it wrong once invalidated a whole generation of checkpoints (hence the format_version gate). If you carr
   * The reported ELO is self-referential and measures nothing absolute. Each generation's rating is a K-factor update against the previous generation's rating, which came from the same chain, anchored on an arbitrary 1200 with no fixed reference player. There is no external baseline anywhere except unif
   * The published run had plateaued. checkpoints/pool_index.json for generations 63-99 shows win_rate_vs_prev oscillating 0.3625-0.6375 around 0.5 (consistent with noise at n=80, sigma ~5.6%) and ELO wandering inside a ~10-point band, after 51.8M steps. Several late generations are worse than their pred
====================================================================================================
inhabae/Splendor-Zero (a.k.a. "AhinLendor") | relevance 7 | license **Claimed MIT in README, but there is NO LICENSE file in the repository.** `ls` of the repo root shows no `LICENSE`/`LICENCE`/`COPYING`. GitHub will not show a license badge. Legally this is "all rights reserved by default with an unexecuted README claim" — for anything beyond study/idea-transfer you would want the author to add an actual LICENSE file (or ask explicitly). Copying non-trivial verbatim code (the `native_mcts.cpp` search, the encoder, the heuristic) without that is risky; re-implementing the *ideas* is not.
-- algorithm: **AlphaZero with two search variants + an exact endgame solver.**  1. **Training-time search — `run_native_mcts` (`native_mcts.cpp:2132-2851`)**: PUCT MCTS with a shared tree indexed by *action path from the root* (`MCTSNode.child_index[69]`), but **hidden information is re-sampled at the root before every simulation** (`sample_root_hidden_information`, line 706: shuffles all 3 decks and re-draws the opponent's non-public reserved cards from `deck ∪ hidden_reserved` pool). So it is determinized-MCTS-with-a-shared-tree, not true IS-MCTS.    - PUCT: `q + c_puct * P * sqrt(ΣN + eps) / (1 + N)`, `c_puct=1.25`, `eps=1e-8` (`puct_score_for_action`, line 406).    - **Tree parallelism** with per-nod
-- rules_fidelity: **Standard 2-player Splendor only. Hardcoded, not parameterized.**  - `GameState { Player players[2]; ... }` (`game_logic.h:95`). `validatePlayerIndex` rejects anything outside {0,1}; turn advance is `state.current_player = 1 - p` everywhere. No 3p/4p, no teams. - Bank: 4 of each colour, 5 gold (`initializeGame`, `game_logic.cpp:183-188`) — the correct 2p counts, hardcoded. - Cards: the genuine 90-card standard set is embedded verbatim (`standard_cards`, `game_logic.cpp:13-104`) — 40 tier-1, 30 tier-2, 20 tier-3, ids 1-90, max cost 7, max points 5. - Nobles: the genuine 10 standard nobles, all worth 3 points, 5 of them (4,4) and 5 of them (3,3,3) (`standard_nobles`, line 106). **Exactly 3 ar
-- action_space: **69 discrete actions, fixed slots, flat vector** (`game_logic.cpp:826-1027`, `getValidMoveMask` / `actionIndexToMove`):  - `0-11` buy face-up (`tier*4+slot`) - `12-14` buy own reserved (positional slot) - `15-26` reserve face-up - `27-29` reserve blind from deck top (tier) - `30-39` take 3 different (10 fixed triplets, `kTake3Triplets`) - `40-44` take 2 same (color) - `45-54` take 2 different (10 pairs) - `55-59` take 1 - `60` PASS (emitted only when `!emitted_any`, i.e. no other legal action) - `61-65` return ONE colored gem (return phase; returning gold is explicitly rejected — `validateSingleReturnGem`, `game_logic.cpp:360`) - `66-68` choose noble (index into ≤3 available nobles)  Key si
-- network: **A small MLP, not a ResNet/CNN** (`nn/model.py`, 63 lines, PyTorch):  ``` trunk:   Linear(252,256) -> ReLU -> Linear(256,256) -> ReLU tower:   res_blocks x _MLPResBlock(256)   # x + fc2(relu(fc1(x))), then ReLU policy:  Linear(256, 69)          # raw logits, masked downstream value:   Linear(256, 1) -> Tanh   # scalar in [-1,1] ``` `_MLPResBlock` is post-activation residual, no normalization layers at all (no BatchNorm, no LayerNorm). Policy and value head biases are zero-initialized; everything else uses PyTorch defaults.  **Parameter count**: trunk 130,560; each res block 131,584; policy head 17,733; value head 257. - `res_blocks=0` (argparse default): **~148.5 K params** - `res_blocks=5`
-- self_play: **Teacher/learner with a champion registry, not naive latest-vs-latest.**  - Self-play is pure mirror self-play: the SAME model plays both seats, `env.reset(seed)` gives a fresh shuffle per episode, and seed = `seed_base + episode_idx` (`nn/selfplay_dataset.py:165-168`). Because the observation is side-to-move canonical, seat symmetry is handled by the encoding rather than by seat swapping. - **The generating model is the current champion, not the learner** (`selfplay_use_champion_source=True` by default; `_load_current_champion_checkpoint_for_cycle`, `nn/train.py:484`; usage at 1626-1661). Each cycle loads `champions.json`'s newest entry and generates with it; only if no champion exists doe
-- distributed: **CPU-only. There is no CUDA, no DDP, no `torch.distributed`, no AMP anywhere in the repo** (grep for `torch.distributed|DistributedDataParallel|cuda` returns nothing in `nn/` or `spendee/`). Every `device` default is `"cpu"`; `run_selfplay_session` hardcodes `device="cpu"` (`nn/selfplay_dataset.py:201`).  Parallelism is three separate mechanisms: 1. **Process-level self-play**: `SelfPlayWorkerPool` / `_run_selfplay_session_parallel_impl` (`nn/selfplay_dataset.py:516-870`) with `ProcessPoolExecutor(mp_context=spawn)`. Each worker loads the checkpoint itself and is pinned single-threaded (`_configure_worker_runtime`: `OMP/MKL/OPENBLAS/NUMEXPR_NUM_THREADS=1`, `torch.set_num_threads(1)`, `set_n
-- reported_strength: **Claim**: README says "It reached Rank 1 on Spendee." The task brief mentions 2068 Elo; the evidence actually committed (`assets/leaderboard.png`) shows a *different* number.  **What the screenshot actually shows**: Spendee "PLAYERS" leaderboard, `AhinLendor` at Rank 1 with rating **2420** (highest 2420), **80.00% win rate over 125 games, 2-player only** (0 games at 3p and 4p). Runner-up `nuki221` 2412 (4,607 2p games, 54.83%), `bst123` 2403 (223 games, 89.24%). The site's tiers are labelled grandmaster 2000+, master 1850+, expert 1700+. Note the bot's row is rendered twice (two "Rank 1" rows), so the screenshot itself is slightly odd.  **Evidence quality: weak-to-moderate.** - The only art
-- reuse_for_us: **What ports nearly unchanged**  - *Search machinery.* `native_mcts.cpp` is 90% variant-agnostic: PUCT, virtual loss, tree parallelism, leaf batching against a Python evaluator, Dirichlet noise, forced playouts + policy-target pruning, `same_player`-aware backup, the `NativeMCTSSession` with `forced_root_action_idx` (Bootstrap MCTS), and the IS-MCTS info-set map. The only variant-specific pieces are `sample_root_hidden_information`, `collect_winning_threat_reserve_actions`, and the assumption of a 2-player zero-sum scalar value. - *Card / noble data model.* Our card counts are identical to standard Splendor (40/30/20), built from the same "N templates x 5 colour rotations" structure, with th
-- ideas_to_steal:
   * **Fixed-slot action space that never names a card** (`game_logic.cpp:826-1018`). 69 actions total by (a) making over-cap token return a separate one-token-per-decision sub-turn instead of enumerating discard combinations, (b) indexing buy/reserve by board slot (tier,slot) and reserve slot rather tha
   * **`same_player` flag on every tree edge** (`PathStep`, `native_mcts.cpp:54`; backup at 2470-2481 and 1076-1083). Value backup negates only when the turn actually changes hands, so multi-decision turns (return gem, then noble choice, then end of turn) are modelled as ordinary tree nodes with correct 
   * **Information-set keying by observer-canonical raw state** (`state_encoder.cpp:104` `build_raw_state_for_observer` + `InfoSetKey`/`InfoSetKeyHasher`, `native_mcts.cpp:88-105`). A single integer vector built from ONE fixed observer serves as both the NN input and the IS-MCTS transposition key; oppone
   * **Per-simulation root re-determinization inside the native search** (`sample_root_hidden_information`, `native_mcts.cpp:706`). Hidden reserved cards are pooled with the deck, shuffled, and re-dealt while preserving slot identity and public/private provenance — much cheaper and lower-variance than ru
   * **Anti-clairvoyance penalty on blind-deck actions** (`random_keep_penalty_for_deck_reserve_action`, `native_mcts.cpp:547`). A small random PUCT penalty (0.02/0.06/0.12 by tier) on reserve-from-deck counteracts the determinizer's false certainty about the deck top. Cheap, targeted mitigation of strat
   * **KataGo Playout Cap Randomization wired end-to-end** (`nn/selfplay_dataset.py:57-90,186-243`): 25% of moves get 800 sims WITH Dirichlet noise and forced playouts and are the only ones written to the replay buffer; 75% get 200 sims with noise off and are used only to advance the game. Roughly 3x mor
   * **Forced playouts + policy-target pruning** (`forced_playout_target_visits_for_child` line 394, `prune_policy_target_visit_probs` line 1212). Guarantees every root action gets ~sqrt(k*P*N) visits, then removes those forced visits from the exported policy target so the target is not contaminated by m
   * **Blended value target** `z = 0.5*(root_Q_of_most_visited_child + game_outcome)` (`nn/value_targets.py:12`). One line, big variance reduction over pure outcome targets in a game with a hard 80-turn truncation.
   * **Bootstrap MCTS via a persistent search session with a forced root action** (`NativeMCTSSession::advance(n, forced_root_action_idx)` + `restrict_root_mask`, `native_mcts.cpp:1295`; driver `spendee/engine_policy.py:648-706`). Give every legal root move a guaranteed simulation budget *in the same tre
   * **Domain-specific prior boost for opponent-threat blocking** (`collect_winning_threat_reserve_actions`, `native_mcts.cpp:199`). Simulate the opponent's mask one ply ahead; if any face-up buy takes them to >=15 points (accounting for the noble they would claim), multiply the prior of the matching res
   * **Hydratable engine with strict consistency validation** (`py_splendor.cpp:628` `load_state` / `777` `export_state`; `validate_public_state_consistency` at 255). A JSON-ish payload of card ids reconstructs the engine state, and the loader *re-derives* bonuses and points from purchased cards and reje
   * **Shadow-state hidden-information tracker for live play** (`spendee/shadow_state.py`). Maintains `hidden_reserved_tiers[(seat,slot)]` by diffing consecutive observations: when a slot goes empty->hidden, infer the tier from which deck count dropped or which face-up row changed (`_infer_hidden_tier`, 
-- pitfalls:
   * **HARD 2-PLAYER ONLY.** `GameState { Player players[2]; }` (`game_logic.h:95`) and `1 - p` turn flipping is hardcoded throughout `game_logic.cpp`, `state_encoder.cpp`, `native_mcts.cpp`, and `native_endgame.cpp`. The observation has fixed 'cp/op' halves, `validatePlayerIndex` rejects >=2, and termin
   * **Token-cap semantics are the opposite of ours.** This engine lets a player take gems past 10 and then forces a return phase one token at a time (actions 61-65). Our variant BLOCKS the selection; there is no discard and no return phase. So actions 61-65 disappear, the `is_return_phase` flag and its 
   * **Returning gold is not implemented.** `validateSingleReturnGem` (`game_logic.cpp:360`) throws on `t.joker != 0`, so even in real 2p Splendor this engine forbids a legal move. A latent rules bug that would silently propagate if you ported the return logic.
   * **Take-2-different and take-1 are legal only when the bank has EXACTLY 2 (resp. exactly 1) non-empty colours** (`game_logic.cpp:614-632`). A player can never voluntarily take fewer tokens when more are available. Our cap-blocking rule makes short takes legal in many more situations, so this predicat
   * **BUG: the built-in greedy heuristic opponent reads the state vector at stale offsets.** `py_splendor.cpp:298-300` declares `kFaceupStart = 91` and `kBankStart = 223`, but the actual encoder (`state_encoder.cpp:24-25`) uses 97 and 229. These are the pre-252 (246-dim) offsets, off by exactly the 6 by
   * **Reserve always grants a gold if the bank has one** (`game_logic.cpp:718-722`), with no 10-token check — the overflow is cleaned up by the return phase. Our variant grants gold only if `total < 10`, and reserve is a two-phase, non-cancellable UI flow (ENTER_RESERVE then RESERVE_CARD/RESERVE_FROM_DE
   * **Nobles: fixed at 3 slots.** `std::array<Noble,3> available_nobles`, `noble_count <= 3`, and only 3 CHOOSE_NOBLE actions (66-68). Our variant needs n+1 (4 for 3p, 5 for 4p), which widens the action space and the 15-float noble block in the observation.
   * **Nobles are only checked at end-of-turn, after a buy** (`resolveNobleAndEndTurn`, `game_logic.cpp:517`). Our spec checks after EVERY action type. In standard Splendor these coincide (only buying changes bonuses), but if our variant has any other bonus source the assumption breaks.
====================================================================================================
cestpasphoto/alpha-zero-general (Splendor) | relevance 9 | license MIT. The LICENSE file is the one inherited from the upstream project: "MIT License / Copyright (c) 2018 Surag Nair". cestpasphoto's fork adds no separate copyright line, so attribution should name both upstream (suragnair/alpha-zero-general) and this fork. MIT permits copying the Splendor logic, card/noble tables, MCTS and network code into our project with attribution and license notice retained. Note the pretrained `.pt` weights carry no separate license statement — treat them as covered by the same MIT grant but do not rely on them anyway, since they encode the wrong card set for our variant.
-- algorithm: AlphaZero with KataGo-style additions. All in `MCTS.py` + `Coach.py` + `GenericNNetWrapper.py`.  MCTS (`MCTS.py`): PUCT `u = Qsa + cpuct * Ps[a] * sqrt(Ns)/(1+Nsa)`; unvisited actions get `u = fpu_init + cpuct*Ps[a]*sqrt(Ns+1e-8)` where `fpu_init = Qs - fpu` if fpu>0 (parent-relative reduction) else the absolute value `fpu` (MCTS.py:211-230). Node store is one dict `nodes_data[s] -> (Es, Vs, Ps, Ns, Qsa, Nsa, r, Qs)` keyed by `board.tobytes()` (SplendorGame.py:59-60), i.e. a transposition table reused across moves within an episode, garbage-collected when `round > last_cleaning+20` by deleting nodes with `r < round-5` (MCTS.py:86-91).  Value is a VECTOR of length num_players. `search()` recu
-- rules_fidelity: Standard Splendor, 2/3/4 players. Player count is a MODULE-LEVEL CONSTANT `NUMBER_PLAYERS = 2` at splendor/SplendorGame.py:9 — you edit the source and retrain per player count; it is not a runtime argument.  Faithful to real Splendor AND to our variant on these points (splendor/SplendorLogicNumba.py:140-150, 359-470): - Tokens per color = {2:4, 3:5, 4:7}, gold = 5. Exactly our numbers. - Nobles revealed = {2:3, 3:4, 4:5} = n+1. Exactly our rule. - Noble table (splendor/SplendorLogic.py:127-138): 10 nobles, ALL worth 3 points, 5 of the form 4+4 on two cyclically adjacent colors, 5 of the form 3+3+3 on three cyclically consecutive colors. This is structurally IDENTICAL to our bonus-tile set. -
-- action_space: Fixed 81 actions for all player counts (`action_size()` returns 81, line 94-96). Layout (documented at SplendorLogicNumba.py:53-84, decoded by `move_to_str` in SplendorLogic.py:6-50): - 0-11: buy visible card i (tier = i//4, index = i%4) - 12-23: reserve visible card i - 24-26: reserve blind from deck of tier 0/1/2 - 27-29: buy from own reserve slot 0/1/2 - 30-54: take a combination of 1, 2 or 3 DIFFERENT gems — C(5,1)+C(5,2)+C(5,3) = 5+10+10 = 25 entries, enumerated by `itertools.combinations` in `_gen_list_of_different_gems(3)` - 55-59: take 2 identical gems of color 0..4 - 60-74: give back 1 or 2 different gems (C(5,1)+C(5,2) = 15) - 75-79: give back 2 identical gems of color 0..4 - 80: p
-- network: PyTorch, defined in splendor/SplendorNNet.py; version selected by `-V`. All shipped Splendor checkpoints are `nn_version = 80`.  V80 (shipped): input (B, nb_vect, 7). `LinearNormActivation` (Linear(nb_vect, nb_vect, bias=False) applied on the transposed axis + BatchNorm1d), then ONE `InvertedResidual1d` MobileNetV3 block (expand to 3*nb_vect, "depthwise" Linear(7,7) over the 7-wide axis, Squeeze-Excitation with `_make_divisible(exp//4, 8)` bottleneck, project back, residual). Two separate heads, each = another InvertedResidual1d (Hardswish + max-pool SE) then Flatten -> Linear(nb_vect*7, out) -> ReLU -> Linear(out, out). Policy out = 81; value out = num_players, squashed with `tanh`. Policy 
-- self_play: Vanilla AlphaZero self-play: one network plays every seat. `Coach.executeEpisode` (Coach.py:37-84) canonicalizes, runs `getActionProb(temp=1.)`, samples the move with a decaying temperature, and records (board, pi, curPlayer, valids, q) ONLY when the search was a full one (playout-cap randomization). At game end the reward vector `r` from `check_end_game` is rotated into each sample's own frame via `np.roll(r, -curPlayer)`.  Reward shape: `check_end_game` returns a float32 vector of length n — `+1.0` for the sole winner, `-1.0` for everyone else, or `0.01` for all tied players if the fewer-cards tiebreak fails to separate them. It is NOT zero-sum for n>2 (a 4-player game yields [1,-1,-1,-1])
-- distributed: Essentially none, and this is the biggest mismatch with our hardware.  - Training device is HARDCODED to CPU: `self.device = {'training':'cpu', # 'cuda' if torch.cuda.is_available() else 'cpu', ...}` (GenericNNetWrapper.py:27-31). The `cuda` branch in `switch_target` (lines 292-294) is dead code, reachable only by editing that dict. - Inference is ONNX Runtime with `providers=['CPUExecutionProvider']` and `intra_op_num_threads = inter_op_num_threads = 1` (GenericNNetWrapper.py:326-328). The net is re-exported to ONNX on every mode switch, written to a temp file, loaded, then the file is deleted (`export_and_load_onnx`, lines 300-329). - `OMP_NUM_THREADS=1` and `torch.set_num_threads(1)` are 
-- reported_strength: Weak and, for Splendor specifically, almost absent. Be skeptical.  In the main README the Splendor section makes NO strength claim at all — only "Support of Splendor with 2 players", "Support of 3-4 players", "Proper MCTS handling of chance", "Added pretrained models for 2-3-4 players". Every other game in that README carries a number (Santorini 90-95%+ vs Ai Ai / BoardSpace, Akropolis 75%+ vs an "undefeated strategy" YouTube video, Abalone 90%+ vs Aba-Pro level 6, Azul "seems much stronger than PJF98 (~700 rated on BGA)"). Splendor is conspicuously the one without.  The only Splendor number exists in the author's SEPARATE browser-demo repo README (cestpasphoto/cestpasphoto.github.io): ">90%
-- reuse_for_us: This is the single closest existing codebase to our INDIVIDUAL mode. I would port the game logic and the chance-handling, and deliberately NOT port the training infrastructure.  DIRECT DROP-INS (little or no rework): 1. Noble/bonus-tile table. splendor/SplendorLogic.py:127-138 already encodes exactly our set: 10 tiles, all 3 points, 5 x "4+4 on two cyclically adjacent colors", 5 x "3+3+3 on three consecutive colors". Copy verbatim; only re-map which color index pairs count as "adjacent" if our color cycle differs. `num_nobles = {2:3, 3:4, 4:5}` is already n+1, our rule. 2. Token supply. `num_gems_in_play = {2:4, 3:5, 4:7}` and gold 5 — identical to ours, no change. 3. Card tables. `np_all_ca
-- ideas_to_steal:
   * Seeded 'universes' (PC-PIMC determinization) for hidden card draws — MCTS.py:14,63 plus SplendorLogicNumba.py:306-336. Each MCTS simulation picks a seed from a fixed list of 8 magic constants; the deck draw becomes a deterministic function of (seed, remaining-deck bitfield) via `(4594591*(seed+deck_
   * Whole game state as ONE contiguous int8 array with named numpy VIEWS assigned in `copy_state` (SplendorLogicNumba.py:207-219). The code reads like it has fields (`self.bank`, `self.players_gems`, ...) but there is exactly one buffer, so state copy is one memcpy, hashing is `board.tobytes()`, and the
   * Deck contents as an 8-bit BITFIELD per (tier, color), packed/unpacked with `my_packbits`/`my_unpackbits` (SplendorLogicNumba.py:103-110, 165-167, 313-328). Exact remaining-card tracking in one byte per color, and it doubles as the hash input for the universe seed. Our variant's 8/6/4 templates per c
   * Numba jitclass for rules + `@njit(nogil=True)` for the MCTS inner loop, with select+step+canonicalize fused into ONE njit call that takes the jitclass as an argument (`get_next_best_action_and_canonical_state`, MCTS.py:233-248). Because `nogil=True` releases the GIL, N Python threads can genuinely r
   * Per-player VALUE VECTOR (head outputs num_players tanh values) plus `np.roll(v, next_player)` on the way back up the tree and `np.roll(r, -curPlayer)` on the training targets (SplendorNNet.py:276-279 / MCTS.py:175-176 / Coach.py:76-82). This is the minimal correct change that turns 2-player AlphaZer
   * Board-state symmetry augmentation specific to card games: permute the 4 face-up cards within a tier and apply the SAME permutation to both the buy-action block (`4*tier..`) and the reserve-action block (`12+4*tier..`) of the policy and validity vectors; separately permute own reserved cards (`get_sy
   * Playout Cap Randomization done right: `prob_fullMCTS=0.25`, `ratio_fullMCTS=5`, and — the part people forget — ONLY full searches are written to the replay buffer (`if is_full_search:` at Coach.py:65). Cheap searches still advance the game and still generate opponent pressure, but never poison the p
   * Forced playouts + policy target pruning with k=0.5, implemented in ~6 lines: during search `if Nsa[a] < int(sqrt(k*Ps[a]*n_iter)): return a` (MCTS.py:218-220), and at the root subtract `int(sqrt(k*Psa*sims))` from every non-max count and zero anything <=1 (MCTS.py:75-80). Combats policy collapse wit
   * Q/Z blended value target: `target = (Z + q_weight*Q_root)/(1+q_weight)` where Q_root is the MCTS root value estimate (GenericNNetWrapper.py:188-190), with q_weight annealed across the training curriculum (README shows 0.0 -> 0.5 -> 0.5 -> 1.0 as sims go 100 -> 200 -> 400 -> 800). Cheap variance redu
   * Action masking baked into the exported inference graph: `valid_actions` is a second bool network INPUT, applied as `torch.where(valid, logits, -1e8)` before `log_softmax` (SplendorNNet.py:404), and it appears in the ONNX graph as a `Where` node before `LogSoftmax`. The deployed model can never propo
   * Multi-step turns without leaving the AlphaZero framework: `make_move` returns the SAME player index when the turn is not yet complete (smallworld/SmallworldLogicNumba.py:238-240). This is the pattern for our ENTER_RESERVE -> RESERVE_CARD sequence and for CHOOSE_TILE when several bonus tiles qualify.
   * MCTS tree as a transposition table keyed on the exact state bytes, reused ACROSS moves within a game, with cheap age-based GC (delete nodes whose stored round < current-5, every 20 rounds; MCTS.py:86-91). In Splendor many action orders transpose, so this is real savings, and it removes the need for 
-- pitfalls:
   * No strength evidence for Splendor. The main README makes zero strength claims for Splendor while every other game gets a number. The only figure anywhere — '>90% winrate against Lapidary AI, 10 wins in 10 games' — lives in a different repo (cestpasphoto.github.io), is n=10 against one hobby AI, with
   * Training and inference are CPU-only. `self.device['training'] = 'cpu'` is hardcoded at GenericNNetWrapper.py:27-31 with the cuda line commented out; ONNX runs with `CPUExecutionProvider` and 1 intra/inter-op thread. There is no DDP, no multi-node, and README_features lists multi-core self-play as ST
   * Player count is a compile-time constant. `NUMBER_PLAYERS = 2` at splendor/SplendorGame.py:9 must be edited and the code reimported; the observation shape, the value head width, and the checkpoint all change with it. You get three separate models and three separate source states, not one configurable
   * Noble/tile logic does not match our rules in three ways at once: it awards EVERY qualifying noble simultaneously (SplendorLogicNumba.py:465-470), gives the player no choice, and is only called from `_buy_card` so it never fires after a gem take or a reserve. Our variant awards one, lets the player c
   * An always-legal PASS (action 80, `result[80] = True` at line 187) plus standalone 'give back gems' turns (actions 60-79) are both non-rules in our variant. Worse, removing the pass exposes a latent crash: `pick_highest_UCB` returns `best_act = -1` on an all-zero mask, and our variant genuinely can r
   * Deck bitfield rows are fed to the shipped network as raw magnitudes. `my_packbits` produces 0-255 but the array is int8, so a full 8-card deck stores as -1, not 255 (SplendorLogicNumba.py:104-106, 167). V80/V100 flatten the entire 56x7 board including rows 26/28/30, so the net sees a bit-packed fiel
   * Observations are fed completely unnormalized: `torch.FloatTensor(boards.astype(np.float32))` with no scaling, no one-hot, no embedding for V80 (GenericNNetWrapper.py:62). Mixing 0-7 token counts with -128..127 bitfields and a 0-255 round counter in the same tensor. Fine for a 142k-param net that ove
   * The round counter is stored in `bank[0][idx_points]` as an int8 read back through `.astype(np.uint8)` (line 303-304). It wraps at 256 and is only safe because `max_moves = 62*num_players <= 248`. Our variant's longer/team games could exceed that; widen the field.
====================================================================================================
seal256/splendor — MCTS with Policy Network for Splendor | relevance 7 | license NO LICENSE. There is no LICENSE/COPYING file and no license header in any source file (verified by directory listing and grep). The only third-party component is a vendored `src/json.hpp` (nlohmann/json, MIT). Under default copyright this repo is "all rights reserved" — reading it for ideas is fine, but copying source into our project would require asking the author (github.com/seal256) for an explicit license grant. Safe path: re-implement the ideas rather than lift the C++ files.
-- algorithm: SEARCH (src/mcts.cpp, 360 lines; mirrored in pysplendor/mcts.py): - Four variants sharing one base: MCTS (plain UCT + random rollout), PolicyMCTS (NN prior, PUCT-style), ValueMCTS (rollout blended with a value fn), PVMCTS (both). Only MCTS and PolicyMCTS are used in the shipped configs. - Nodes store ONLY the action, parent pointer, acting_player, visits, wins, prior p — no game state. Each iteration does `state = root_state->clone()` and replays actions down the path (mcts.cpp:29-33). Deliberate memory/copy tradeoff documented at pysplendor/mcts.py:21-25 ("game states are big, next state computation is relatively cheap"). - Expansion is delayed: a leaf is only expanded on its SECOND visit (
-- rules_fidelity: STANDARD 2-4 player Splendor with a handful of simplifications. Rules live in `SplendorGameRules` (src/splendor.h:94-111 and pysplendor/splendor.py, identical): - num_players 2/3/4; max_open_cards=4; max_hand_cards=3 (reserve limit); win_points=15 (overridable per-run via the `win_points` JSON field — the trained model uses 5); max_player_gems=10; max_nobles = num_players + 1; max_gems_take=3; max_same_gems_take=2; min_same_gems_stack=4; max_gold=5; token stack per color = 7 for 4p, else 2+num_players (4 for 2p, 5 for 3p). - Cards: the real Splendor deck, 40/30/20, hardcoded as strings in src/splendor.cpp:18-22 (also assets/cards.csv, 89 rows + header). Format "[k0|r1g1b2w1]" = black card, 0
-- action_space: Flat, fixed, global action id list of 83 strings (src/splendor.cpp:28-34; mirrored pysplendor/splendor.py:230, PLAYER_ACTIONS_STR at :238): - 1  "s"  — skip (only legal when nothing else is) - 5  "tr2","tg2","tb2","tw2","tk2" — take 2 identical (requires stack >= 4) - 10 "tr1g1b1" ... "tb1w1k1" — take 3 distinct, C(5,3) - 12 "r{level}n{pos}" — reserve face-up card, 3 levels x 4 positions - 12 "p{level}n{pos}" — purchase face-up card - 3  "h0","h1","h2" — purchase from hand (reserved) - 40 "c0".."c39" — CHANCE actions, index into the remaining deck array  => 43 PLAYER actions (network output size, `NUM_ACTIONS = 43` in train.py:17) + 40 chance actions never predicted by the network.  MASKING:
-- network: Tiny MLP, PyTorch, defined in train.py:72-92. - `MLP(input_size=1052, hidden_size=512, hidden_layers=2, out_size=43)`; ReLU; `F.softmax` applied INSIDE forward() (train.py:91) so the exported module emits probabilities. - Parameter count with the shipped defaults: 1052*512+512 = 539,136; 2 x (512*512+512) = 525,312; 512*43+43 = 22,059 → ~1.09M parameters. Roughly 4 MB fp32 — trivially small for a 3060, and far below what 4xA100 could train. - Xavier-uniform init, zero biases. - An alternative `ResNet` (train.py:114-134: initial FC + N residual MLP blocks, final FC zero-initialized) is implemented but commented out at the call site (train.py:264). - NO VALUE HEAD anywhere in the trained pipel
-- self_play: - Pure symmetric self-play: both seats are the SAME current-best model. `_collect_self_play_data` runs best vs best for train (train=True, i.e. sampled root moves) and best vs best for val (train=False, greedy) — self_play.py:132-137. - No league, no opponent pool, no historical-snapshot mixing. The only "opponent diversity" is the 10-iteration replay buffer of past self-play data. - Gated promotion: the freshly trained model plays 1000 games against the current best with `rotate_agents=True`; promoted only if win rate > 0.54 (self_play.py:93, 166-172). Otherwise the best model is kept and an iters_without_improvement counter advances (stop at 12). - Seat handling: `rotate_agents` cyclically
-- distributed: Essentially nothing reusable for a 4xA100 node — this is a single-machine, CPU-thread-parallel design. - Parallelism = `std::async(std::launch::async, ...)` fan-out of `num_workers` threads, each running `num_games/num_workers` complete games sequentially, then a join (src/game.cpp:118-138). Configs use num_workers 9-10 (author's laptop); the shipped self_play_config.json sets num_workers=1. - Remainder games are silently dropped (`num_games / num_workers` integer division). - Reproducibility is explicitly broken under threading: "[warn] do not lead to reproducible games in parallel execution setup (num_workers > 1)" (src/game.h:37). RNG is `std::rand()` seeded once globally (src/main.cpp:44
-- reported_strength: All claims are README-level with modest evidence. Concretely:  BASELINE LADDER (README table, 2-player, 1000 games/pair from baselines.py): - MCTS with unrestricted chance nodes beats Random 0.91 +/- 0.02 → "+400 +/- 40 Elo" - MCTS-500 (max_chance_children=1) beats MCTS-unrestricted 0.89 +/- 0.03 → "+360 +/- 55 Elo" - MCTS-2000 beats MCTS-500 0.66 +/- 0.04 → "+115 +/- 30 Elo" README then says "MCTS is approximately 750 Elo points stronger than a random player (win-points-5 setup, 500 MCTS iterations per move)" — which is the chained sum 400+360, but baselines.py as committed uses GameConfig defaults with win_points=15, so the stated win-points-5 attribution does not match the committed scrip
-- reuse_for_us: DIRECTLY REUSABLE CONCEPTS (re-implement, do not copy, given the missing license):  1. The chance-node contract and the max_chance_children knob. Our variant has three chance sources (face-up refill after buy/reserve, RESERVE_FROM_DECK top card, and the initial deal). Modelling them as explicit CHANCE_PLAYER=-1 nodes in the same GameState interface, then capping children at 1, is the single most transferable idea. Implementation cost: low. But we must NOT copy the backprop reward division (see pitfalls) — pass rewards up unchanged and let visit-count averaging do the expectation.  2. Selection-blocking token cap. seal256 already implements exactly our rule: no discard step, illegal over-cap 
-- ideas_to_steal:
   * Explicit chance nodes in the GameState interface: active_player() returns CHANCE_PLAYER=-1 whenever a deck draw is pending, and get_actions() enumerates one action per remaining deck card. Keeps stochasticity out of apply_action and makes the search algorithm game-agnostic. src/game_state.h:29 + src
   * max_chance_children: shuffle a chance node's children then truncate to K (K=1 in every shipped config), turning a ~30-way stochastic branch into a single sampled determinization. src/mcts.cpp:150-153. This is the repo's headline result and the cheapest possible way to keep effective search depth in 
   * Nodes store only the action, not the state; each iteration clones the root state and replays the path down. Combined with a shallow-copy clone (cards are const pointers into immutable static tables) this makes the tree tiny and the copy cost trivial. pysplendor/mcts.py:21-25 (rationale) + src/splend
   * Delay expansion to the second visit of a leaf: the first visit is rollout-only. One line (`node->visits > 0` in the expansion guard, src/mcts.cpp:36) that halves node allocation in a high-branching game.
   * Shuffle children at expansion, then in select_child return the first child with visits==0. Gives random first-play-urgency ordering without any extra RNG in the hot loop. src/mcts.cpp:120-124 + 150.
   * Bit-packed binary features: because the encoding is pure 0/1, store datasets with np.packbits and expand with np.unpackbits(count=state_len) at load. 8x memory/IO saving with two lines. prepare_features.py:103 + train.py:42.
   * Rotate the player blocks so the active player is always encoded first: player_idx = (n + active_player) % num_players. Free symmetry exploitation, no extra parameters. src/splendor_agents.cpp:116.
   * Drop constant information from the encoding: nobles are all worth 3 points so the points field is omitted entirely ('A noble always gives you 3 points, no need to encode them'). Applies verbatim to our bonus tiles. src/splendor_agents.cpp:67-70.
   * Cross-implementation differential tests: dump trajectories from the fast engine, replay them through the reference engine, and assert both the state sequence AND the encoded feature vector AND the model prediction match bit-for-bit. tests/state_encoder_test.py:50-51, tests/pysplendor_test.py. For us
   * Compact replayable trajectory format: {initial_state, agent_names, actions, rewards, optional per-move MCTS visit freqs}. Any downstream statistic is recovered by replaying actions from the initial state rather than storing states. src/game.h:16-23, src/main.cpp:109-121.
   * Report the target distribution's own entropy as an irreducible loss floor before training, so you can tell whether the policy loss is actually improving or just approaching the noise floor of the MCTS visit targets. train.py:140-146.
   * win_points as a first-class per-run rules override, enabling a shortened-game curriculum (his self-play only converged at 5 instead of 15). src/game.cpp:74, 93-96.
-- pitfalls:
   * CRITICAL — the chance-node backup divides rewards by the number of chance children. In backpropagation, every time the path crosses a chance edge, `for (auto& r : rewards) r /= bp_node->parent->children.size();` (src/mcts.cpp:54-59, identically pysplendor/mcts.py:77-79). For a SAMPLED backup this is
   * The NN prior normalization is a no-op. src/nn_policy.cpp:55-57 writes `for (auto p : action_probs) { p /= sum_probs; }` — `auto` copies by value, so nothing is written back. PUCT priors are therefore raw softmax entries over all 43 actions restricted to the legal subset, summing to well under 1, whi
   * Feature clipping destroys card costs. gems_to_vec clips every count at rules.max_gems, which is 4 for 2 players and 5 for 3 (src/splendor_agents.cpp:43). Real Splendor tier-3 costs go to 7, so in 2-player games all cost components >= 4 are indistinguishable to the network. Worse, the encoding's mean
   * get_actions() and verify_action() disagree on the take-2 cap. get_actions requires `player_gems_sum < max_player_gems - max_same_gems_take` (sum <= 7) while verify_action correctly requires `sum + 2 <= 10` (sum <= 8) — src/splendor.cpp:351 vs :497. A player at exactly 8 tokens is wrongly denied a ta
   * No torch::NoGradGuard / c10::InferenceMode on the C++ inference path (src/nn_policy.cpp:20-40, confirmed absent by grep). Every batch-1 forward builds an autograd graph. Free speedup left on the table, and a memory-growth risk under long-running multi-threaded collection.
   * std::rand() used as the global RNG and to seed per-thread mt19937 (src/main.cpp:44, src/random_util.h:9,19, src/mcts.cpp:116/165 `rand() % n`). Not thread-safe, and the header carries the author's own 'TODO: get rid of std::rand()'. The task struct documents that parallel runs are not reproducible (
   * Dirichlet noise is effectively disabled in the shipped self-play recipe: MCTSParams defaults are p_noise_level=0.25/alpha=0.03, but scripts/splendor_job.py:30-31 sets 0.0/1.0 and sample_configs/self_play_config.json sets p_noise_level=0.0. Anyone reading the class defaults will misreport what was ac
   * Exploration temperature never anneals. weighted_selection_moves=20 counts ROUNDS (move_num() returns `round`, src/splendor.cpp:654-656) and wp5 games last ~15-18 rounds, so the entire self-play game is played at temperature 1 with visit-proportional sampling. Fine for a 5-point game; would badly wea
====================================================================================================
Nikola995/Splendor-AI ("SplendorRL") | relevance 3 | license NONE. There is no LICENSE, COPYING, or license header in any branch — I checked. The README's commented-out sections contain a struck-through MIT badge and an MIT reference, but both are inside HTML comments and therefore not in effect. Under default copyright this is All Rights Reserved. The README's "Notice" section further states the project is "not intended for commercial use or distribution" and is "for educational purposes only". Practical consequence: do NOT copy code from this repo into our project. Everything worth taking is a design idea (an index layout, an ordering convention) rather than an expression, so re-implement from the description; do not paste.
-- algorithm: None. There is no learning algorithm of any kind — no policy gradient, no value function, no MCTS, no search, no loss, no optimizer, no hyperparameters. The single "agent" is `RandomModel` (/tmp/claude-0/-home-user-08162026/6c4d3121-5cae-5177-9eec-d5961b7f3014/scratchpad/research/Nikola995__Splendor-AI, branch origin/v1, agents/models.py): `def evaluate(self, game_state=[]) -> list[float]: return [random() for _ in range(self.output_size)]` with `output_size: int = 42`. The action-selection wrapper in origin/v1:agents/agent.py is the one design idea present: it argsorts the 42 scores descending and returns the FIRST action that passes `game.can_make_move_for_current_player(action)`, i.e. "re
-- rules_fidelity: Standard 2-4 player Splendor only, individual scoring, and the rules core is faithful and well-tested. Verified by running it: - Cards: 90 total, 40/30/20 by tier — I loaded the shipped pickle and counted. Source data is game_base/splendor_cards_list.csv (90 rows, columns Level, Gem color, PV, (w)hite, bl(u)e, (g)reen, (r)ed, blac(k)); `CardGenerator.generate_cards()` prefers game_base/cards_manager_collection.pkl and falls back to the CSV via pandas. 4 face-up per tier (`CardManager.table_size = 4`); replenishment from `deck.pop()` in `remove_card_from_table`. - Tokens: `Bank.__post_init__` sets regular = 7/5/4 for 4/3/2 players, gold fixed at 5. Matches our variant's per-color counts exact
-- action_space: This IS the one genuinely reusable artifact, and it exists only on the unmerged `v1` branch. Fixed size 42, laid out as three contiguous blocks in `StandardActionSet` (origin/v1:game_base/action_sets.py): - indices 0-14 (`token_actions`, built once at construction and immutable for the whole game): 10 x `Reserve3UniqueColorTokens` = C(5,3) combinations of the 5 non-gold colors, then 5 x `Reserve2SameColorTokens`, one per color. I verified `len(generate_standard_token_actions()) == 15`. - indices 15-38 (`card_actions`, refreshed from the 12 face-up slots): 12 x `ReserveCard` then 12 x `PurchaseCard`, in `get_all_cards_on_tables()` order (tier1 slots 0-3, tier2 4-7, tier3 8-11). - indices 39-4
-- network: None. No PyTorch, no TensorFlow, no JAX, no numpy. `requirements.txt` and `environment.yml` list only python 3.10, pandas, pytest, pygame. The only "model" is a `typing.Protocol` named `Model` with `evaluate(game_state: list[int]) -> list[int]`, and the only implementation is `RandomModel` returning 42 `random.random()` floats. Zero parameters.
-- self_play: No self-play in any RL sense — no opponent pool, no league, no snapshotting, no Elo, no seat randomization, no frozen-opponent evaluation. `origin/v1:splendor_random_agent_sim.py::simulate_game` instantiates ONE `Agent(RandomModel())` and uses that same stateless agent for every seat in the game (`players = [Player(f'random_{i+1}') for i in range(num_players_in_game)]`, then a single `agent.get_action(game)` in the loop). Seat handling is a plain round-robin: `Game._end_player_turn` increments `curr_player_index` and wraps to 0, with seat 0 always starting; `Game.change_player_order` exists but is `raise NotImplementedError()`. Multi-player handling is "N independent scorers"; there is no te
-- distributed: None. No multiprocessing, no vectorized envs, no DDP, no GPU code, no batching, no `SubprocVecEnv`-style wrapper, no throughput numbers reported by the author. `run_simulations` is a plain list comprehension over `tqdm(range(num_simulations))` — strictly serial, single process. `main()` sets `num_simulations = 100_000` per player count for 2, 3 and 4 players, which at the speed I measured would take roughly 15-20 minutes per player-count on one core; there is no evidence in the repo (no committed data/simulations/random_choice.csv, no results in the README) that this was ever run. I benchmarked the env myself (Python 3.11, single core, pandas stubbed out since the shipped pickle makes it unn
-- reported_strength: No strength is reported and none could be — there is no trained agent. Treat the README with skepticism on two counts. First, the project title "SplendorRL: A Reinforcement Learning Environment for Splendor" and the framing "training of reinforcement learning agents as skills" describe an aspiration, not the code; the Key Features list is honest about this (only three items are checked: rules recreation, customizable game parameters, human CLI; the PettingZoo integration, agent CLI, "Training and comparison of different RL algorithms", and GUI are all explicitly unchecked). The orchestrator's brief for this study — "SplendorRL environment with PettingZoo integration" — is exactly the misread
-- reuse_for_us: Almost nothing ports as code (no license, no RL stack, and the rules are standard Splendor rather than our variant). Reuse is limited to four design decisions, re-implemented from scratch: WHAT MAPS DIRECTLY: 1. The 10-token cap as selection-blocking. `Player.can_add_token` (`sum(held) + sum(taking) <= 10`) with every token action's `can_perform` calling it, and NO discard step anywhere, is precisely our variant's "hard 10-token cap with no discard; selection is blocked". This is the one rule where this repo is closer to us than to official Splendor, so it is worth copying the shape: the cap lives in the mask, not in a post-action correction. Note it also correctly gates the reserve gold (`R
-- ideas_to_steal:
   * Fixed-size flat action index with contiguous typed blocks, decoded by range arithmetic. origin/v1:game_base/action_sets.py `StandardActionSet.get_action_by_idx` — [0,15) token actions, [15,39) card actions over the 12 face-up slots, [39,42) buys from own reserve; the token block is built once at con
   * A first-class boolean legality mask over the whole action index, computed from the same predicate the mutator uses. origin/v1:game_base/games.py `Game.all_actions_legality() -> list[bool]` = `[self.can_make_move_for_current_player(a) for a in self.possible_actions.all_actions()]`. The important part
   * The `can_X()` / `X()` command-query split applied uniformly, with the mutator re-asserting its own precondition. game_base/banks.py, game_base/players.py, game_base/games.py — e.g. `Bank.remove_token` opens with `if not self.can_remove_token(...): raise ValueError`, `Player.add_token` re-checks the 
   * 'Average rank of the chosen action' as a policy-health diagnostic. origin/v1:splendor_random_agent_sim.py returns `idx_sum/num_moves` per game as `Avg_Action_Idx` and reports 'the actions chosen were on average the Nth legal move'. Re-framed for a masked policy, this is a cheap per-rollout metric fo
   * Tracking 'did this game terminate at all' as a first-class rollout outcome. origin/v1:splendor_random_agent_sim.py returns `game.meta_data.state == GameState.FINISHED` and reports a 'stalemate' percentage. Given our variant genuinely admits a no-legal-move state (10 tokens, 3 reserved, nothing affor
   * Opportunistic, non-blocking gold on reserve. game_base/actions.py `ReserveCard.perform` grants the wildcard inside `if bank.can_remove_token({YELLOW:1}) and player.can_add_token({YELLOW:1})` — the reserve itself never fails for want of gold. This is exactly our variant's semantics and is the right p
   * Stateless recomputation of the end-game trigger. game_base/games.py `is_final_turn()` re-evaluates `any(player.prestige_points >= 15)` at the end of every round rather than latching a flag. This is the correct shape for our TEAM 2v2 mode, where the final round is REVOCABLE if the qualifying conditio
   * Ship a preprocessed binary card table and make the parser a fallback. game_base/cards.py `CardGenerator.generate_cards()` loads game_base/cards_manager_collection.pkl if present and only falls back to the pandas CSV parse otherwise. The benefit is real (I ran the whole env with pandas stubbed out to
-- pitfalls:
   * The name is the trap: 'SplendorRL' contains no RL. Before spending any time here, note that grepping every branch for pettingzoo|gymnasium|gym|torch|tensorflow|stable_baselines yields a single hit — the README's own UNCHECKED to-do bullet '- [] Integration with the PettingZoo framework'. Any downstr
   * The observation function does not exist. origin/v1:game_base/games.py `state_matrix()` is `# TODO: fill the matrix; return []`, and game_base/game_interface.py `GameInterfaceAgents` has every method as `pass`. Do not budget any time saved on state encoding.
   * The only enumeration API on main/develop is broken. `StandardActionSet.legal_actions` builds `PurchaseCard(None)` from the `[None]*3` reserve placeholders and dies with `AttributeError: 'NoneType' object has no attribute 'token_cost'` at game_base/action_sets.py:73 on any fresh game — I reproduced i
   * Missing take-fewer-than-3-tokens is not a cosmetic gap — it is the dominant source of unfinished games. With the repo's action set, 67.8% of 4-player uniform-random games (400 sampled) end in a no-legal-move deadlock; adding take-1/take-2-different drops it to 41.5%. In 62% of the 4p deadlocks the b
   * The 42-action index goes stale on v1. `Game.make_move_for_current_player` refreshes `card_actions_player` only inside `if hasattr(action, 'card')`, so after any token action indices 39-41 still address the previous player's reserved cards. Our equivalent must recompute the slot->entity binding uncon
   * `CardManager.fill_table()` destroys the table. With one empty slot it pops 4 cards and overwrites all 4 entries — I measured deck 36 -> 32 and all three previously-occupied slots changed. It is only safe today because it is called exactly once from `Game.initialize()`. If we port the CardManager sha
   * No seeding anywhere. `from random import shuffle` at module scope in game_base/cards.py and game_base/nobles.py; zero hits for seed|Random( across game_base/. Games are irreproducible and per-worker independence is impossible. Our env needs an explicit `np.random.Generator` (or per-env PCG) threaded
   * Speed is ~2 orders of magnitude short of what 4x A100 wants. I measured 6,549-6,880 moves/s and 87-113 games/s on one core, with 103 us of the ~108 us step budget spent on the 42-action mask. The single worst offender is `Game.can_make_move_for_current_player` doing `action.card not in self.cards.ge
====================================================================================================
filipmlynarski/splendor-ai | relevance 3 | license MIT, Copyright (c) 2018 Filip Mlynarski (LICENSE, 20 lines). Permissive -- any file or idea here can be copied into our codebase with attribution. Note environment/cards.csv and nobles.csv reproduce the published Splendor card set (game rules/data are not copyrightable, but the card art/branding is not included here, only numbers).
-- algorithm: Online tabular-style Q-learning with a Keras MLP as function approximator ("DQN" without any of the DQN machinery). Core loop: /home/user/.../filipmlynarski__splendor-ai/splendor_ai/train_model.py:628-744.  Per environment round: choose_action (train_model.py:460-481) does epsilon-greedy over a legality-masked value vector; step() executes; then four opponent models move; then ONE gradient step: ``` r = reward(new_s, s) target = r + this_brain['y'] * np.max(next_state_pred*aval_vector) target_vec = next_state_pred[0]; target_vec[a] = target model.fit(new_x, target_vec.reshape(-1, output_nodes), epochs=1, verbose=0)   # train_model.py:679-686 ``` Note `new_x` is the NEXT state, so it fits Q(s
-- rules_fidelity: Standard Splendor, 4 players ONLY, implemented in environment/splendor.py (543 lines, pandas DataFrames as card store).  Constants (splendor.py:12-18): GOLD=5, NOT_GOLD=7 tokens/colour (4-player counts only, no 2p/3p scaling), NOBLES=5, NOBLEMAN_VALUE=3, WINNING_SCORE=15, MAXIMUM_TOKENS=10, MAXIMUM_RESERVATIONS=3. Players hardcoded to 4 (splendor.py:541), turn order `(current_player + 1) % 4` (splendor.py:178).  Deck (environment/cards.csv, verified by counting): 90 cards, schema `tier,value,type,green,white,blue,black,red`; tier1=40 (35 worth 0pt, 5 worth 1pt), tier2=30 (10x1pt, 15x2pt, 5x3pt), tier3=20 (5x3pt, 10x4pt, 5x5pt). Nobles (environment/nobles.csv): 10 total -- 5 of the "4+4 of tw
-- action_space: Flat 88-way discrete head, index layout fixed in `Splendor.output_nodes` (splendor.py:35-50) and decoded in `step` (train_model.py:346-422): - 0-9: take 3 different colours, C(5,3)=10 combinations - 10-19: take 2 different colours, C(5,2)=10 - 20-24: take 2 of the same colour, 5 - 25-29: take 1 of a colour, 5 - 30-53: 3 tiers x 4 board slots x {buy, reserve} = 24 (even offset = buy, odd = reserve) - 54-56: buy reserved card 0/1/2 - 57-86: return tokens, same 30-way combinatorial layout as picking (dead code, see rules) - 87: empty move / pass  Masking: `Splendor.avaliable_outputs()` (splendor.py:389-473) builds a 0/1 vector directly from the live env state -- token-cap thresholds (<=7 / <=8 
-- network: Keras Sequential MLP, `create_model` (train_model.py:533-550), all dense, no conv/attention/embeddings: Dense(1312, linear, input_dim=1312) -> Dense(1312, sigmoid) -> Dense(700, sigmoid) -> Dense(700, sigmoid) -> Dense(88, linear). `hidden_layer_size = int((input_nodes+output_nodes)/2) = 700` (train_model.py:564). Total ~4.92M parameters (4,916,800 incl. biases). Loss MSE, optimizer Adam defaults. A LeakyReLU import exists but every use is commented out (train_model.py:19, 520, 542) -- so it is a four-layer sigmoid stack with a linear first layer, i.e. numerically the worst plausible choice for a value head over unnormalised integer counts.  Checkpointing saves weights only (`model.save_weig
-- self_play: Frozen-snapshot self-play, no league, no opponent pool, no seat handling.  Opponents are created as copies of the learner (`dumb_models = [create_model(...) for _ in range(4)]`, train_model.py:603, or reloaded from disk at :580) and refreshed from the learner's latest checkpoint every `renew_dumb_ai` episodes (train_model.py:726-728): ``` if i%this_brain['renew_dumb_ai'] == 0 and i != 0:     dump_model(model, model_name)     dumb_models = [load_model(model_name) for _ in range(4)] ``` With the default num_episodes=100 and renew_dumb_ai=400 this never fires -- opponents stay at random initialisation for the entire default run.  Critical bug: there are FOUR opponent models in a FOUR-player gam
-- distributed: None whatsoever. Single process, single thread, CPU-oriented Keras/TF1 (keras 2.1.6 per the recovered model JSONs). No torch, no multiprocessing, no vectorised envs, no numba/Cython, no GPU or CUDA references, no DDP, no batching of any kind -- `model.predict` is called on batch-1 arrays several times per move and `model.fit(..., epochs=1)` on a batch of 1 once per round. The environment stores cards as pandas DataFrames and does `deepcopy(new_s)` of the whole state every round (train_model.py:687), plus `deepcopy` of the player list in `return_state(shift=False)`.  The only throughput figure in the repo is the author's own wall-clock estimate: `training_s = 15*num_episodes` (train_model.py:
-- reported_strength: None. The README makes no strength, win-rate, Elo or benchmark claim of any kind -- it contains only install, train and play instructions (24 lines), and the full README history (9 edits) never contained a results section. No trained weights ship with the repo (.gitignore excludes `splendor_ai/brain*`; earlier checkpoints were deleted in commits 071a48d and de434c1).  Evidence quality: essentially zero. The largest evidenced training run is the historical 4,000 self-play games against a frozen copy of a 47,644-parameter single-hidden-layer MLP, with no baseline comparison. The default configuration shipped at HEAD is 100 episodes. Given the wrong-state Bellman target, the cumulative-not-delt
-- reuse_for_us: Verdict: reuse ideas and the data-file shape, port no code. The training stack is unusable for us (single-process Keras 2.1.6, batch-1 online Q-learning, no replay/target net, ~0.07 games/s) and would be a total rewrite for 4x A100 DDP. The environment is a clean read but embeds several rules bugs and is hardcoded to 4 players, so re-deriving ours from server/gameLogic.js is faster than fixing theirs.  What maps directly: - Ego-centric rotation (splendor.py:62-67) is exactly what we want: emit the observation with the actor at index 0. For our team 2v2 we extend it to (me, teammate, opp_left, opp_right); for ONE_V_TWO to (me, then the two others in seat order) plus a role flag for the solo s
-- ideas_to_steal:
   * Fixed 11-dim per-card vector [point value, 5-dim bonus-colour one-hot, 5-dim cost] -- `tier_to_vector2` / `card_to_vector2`, splendor_ai/train_model.py:285-303. The right shape for a per-card slot embedding; for us extend to ~13 dims with an 'empty slot' flag and a 'hidden (deck-reserved)' flag so t
   * Thermometer/unary token encoding -- `tokens_to_vector`, splendor_ai/train_model.py:317-337: each colour's count 0..7 as 7 monotone bits (gold as 5). Makes threshold predicates ('stack has >=4 so a double is legal', 'I am at 10 tokens') linearly separable. Cheap ablation for our encoder; it was writt
   * Put the legal-action mask inside the environment, not the agent -- `Splendor.avaliable_outputs()`, environment/splendor.py:389-473, sharing the exact index layout with the decoder `step()` in train_model.py:346-422. Mask and step can never drift. We should go further and assert mask[a]==1 inside ste
   * Its token-cap masking thresholds are literally our rule -- environment/splendor.py:395-412 gates 3-picks at <=7 held tokens, 2-picks/doubles at <=8, singles at <=9. That is selection-blocking with no discard, exactly our 10-token cap, and it also naturally permits the forced shorter takes our varian
   * Ego-centric state rotation so one network serves every seat -- `return_state(shift=True)`, environment/splendor.py:62-67 rotates the player list so the actor is index 0. Essential for our 2/3/4-player and team modes; extend the rotation key to (me, teammate, opponents) for 2v2.
   * Frozen-snapshot self-play with a periodic refresh cadence -- `renew_dumb_ai` at splendor_ai/train_model.py:726-728 checkpoints the learner and reloads it as all opponents. The seed of a proper league: we should keep the pattern but sample opponents from N historical snapshots per game rather than us
   * Per-run hyperparameter + progress registry in JSON -- `brains.json` written/read at splendor_ai/train_model.py:33-46, 571-604, 726-744, storing y/eps/decay/episodes_done per model name so a run resumes with its own decayed epsilon and episode counter. Trivial to reimplement and genuinely convenient 
   * Cartesian-product sweep launcher -- splendor_ai/create_brains.py:13-22 builds the config grid as a list-of-lists and shells out one run per combination. Fine as a 20-line pattern for our own sweeps (though we would submit them in parallel, not sequentially).
   * Card and noble data as CSV with a `tier,value,type,<5 costs>` schema -- environment/cards.csv, environment/nobles.csv. Diff-able, reviewable, and easy to regenerate from server/gameLogic.js and assert against it in a test.
   * Coloured-terminal board renderer -- print_board.py:13-120 (unicode blocks/circles per colour, one line per player, arrow marking the player to move). A ~120-line debugging aid that makes self-play trajectories human-readable; worth reproducing for our own trajectory dumps.
-- pitfalls:
   * The Bellman target is fitted on the WRONG state: `model.fit(new_x, target_vec)` at splendor_ai/train_model.py:686 uses the successor state's features with the target for the action taken in the predecessor state, i.e. it learns Q(s',a) <- r + gamma*max Q(s') instead of Q(s,a). The same bug is presen
   * The reward is a cumulative potential, not a delta: `good_boy = sum(me['cards'].values()) + me['score']**2` (train_model.py:489), so every step returns a large growing positive number and the discounted return diverges -- catastrophically so for the y=1.0 cell of the sweep grid in create_brains.py:17
   * Reward and win/loss are credited to `state['players'][0]` (train_model.py:484, 696-703), which after the ego-centric rotation is the NEXT player to move, not the actor. Rotation and credit assignment must be kept consistent; this is the exact trap our rotation-based encoder will walk into.
   * Five agents move per round in a four-player game: `dumb_models` has 4 entries (train_model.py:580, 603) and all four are stepped after the learner (train_model.py:658). `current_player` advances 5 mod 4 = 1 extra seat per round, so the learner silently plays a different seat every round and shares i
   * Roughly 74% of the observation is dead: `input_nodes` is computed as 346, then overwritten to 966, then to 1312 (train_model.py:73-74), while `state_to_nodes` only ever writes indices 0-345. Derive the vector size from the encoder (or assert encoder-length == declared-length) instead of hardcoding i
   * The card region of the observation self-overlaps: tier1 -> [6:50], tier2 -> [34:78] and again [50:94], tier3 -> [62:106] and again [94:138] (train_model.py:118-134). Verified net effect: tier1 keeps 28 of 44 dims, tier2 keeps 28 of 44 (the 4th face-up card of each is invisible to the net), tier3 is 
   * Masking by multiplication on a linear value head plus the fallback `a = np.argmax(prediction != 0)` (train_model.py:472-479) means that whenever the best masked value is <= 0 the agent plays the lowest-index legal action -- always 'take green+white+blue'. Use additive -inf masking on logits/values, 
   * No replay buffer, no target network, batch size 1, one `fit` per round with `epochs=1` (train_model.py:686). Highly correlated online regression against a moving bootstrap target -- the classic recipe for divergence.
====================================================================================================
BreckEmert/Splendor-AI | relevance 7 | license NONE. No LICENSE file on any branch, no license field anywhere, no SPDX headers. Default "all rights reserved" - we cannot copy code verbatim without asking the author. Ideas and algorithms are of course fine to reimplement, and everything of value here (negamax shared net, relative-cost encoding, take/discard-count factoring, PUCT with determinization, PFSP, shaping anneal) is either standard published technique or trivially reimplementable from a description.
-- algorithm: MAIN BRANCH - Dueling Double DQN, Splendor/RL/model.py (540 lines). ONE shared network plays BOTH seats (training.py:11 `players = [('Player1', model, 0), ('Player2', model, 1)]`); one replay buffer holds interleaved transitions from both seats. NEGAMAX TD target (model.py:272): `targets = rewards - (1.0-dones) * gamma * selected_next_qs` - the minus sign is the whole trick, because next_state belongs to the OPPONENT. There is no seat/perspective flag in the state. Double-DQN: argmax action chosen by the online net on next_states (masked with -inf by the stored legal mask), Q evaluated by the target net. Loss: Huber on a scatter-updated target vector (only the taken action's Q is replaced) -
-- rules_fidelity: Standard 2-player Splendor ONLY. Player count is hard-wired: `self.players[self.half_turns % 2]` (Environment/rl_game.py:29,33). No 3p/4p, no teams, no ONE_V_TWO. Board (Environment/Splendor_components/Board_components/board.py:11): `self.gems = np.array([4,4,4,4,4,5])` - 4 per color (the 2-player count, matching our 2p counts) + 5 gold. 4 face-up cards per tier, refilled from a shuffled deck; deck top is reservable and its identity is not encoded in the state. Cards: the real 90-card Splendor set, loaded from Splendor_cards_numeric.xlsx via pandas (deck.py:43-70). 40 tier-1 / 30 tier-2 / 20 tier-3, each granting +1 permanent discount, costs identical to the base game (Splendor_cards_data.py
-- action_space: 141 flat actions (player.py:65-84), the repo's genuine contribution. Layout: take_dim=96, buy_dim=30, reserve_dim=15. TAKES, indices 0-95: instead of enumerating (take combo x discard multiset) - which explodes - the author enumerates (take combo x NUMBER of discards) and delegates WHICH gems to discard to a heuristic. 10 take-3 combos x 4 discard counts (0-3) = indices 0-39; 5 take-2-same x 3 counts = 40-54; 10 take-2-different x 3 counts = 55-84; 5 take-1 x 2 counts = 85-94; index 95 = backup discard (legal only at exactly 10 gems). Crucially, `_get_legal_takes` (player.py:159-193) marks exactly ONE discard count legal per combo - the forced one, computed as `n_discards = max(0, gems.sum()
-- network: DQN (model.py:152-176): Input(251) -> Dense(512) -> LeakyReLU(0.3) -> Dense(512) -> LeakyReLU -> Dense(256) -> LeakyReLU -> dueling heads: value Dense(1) and advantage Dense(141), combined as `a - mean(a, axis=1, keepdims=True) + v`. HeNormal init throughout. ~0.56M parameters. Keras/TensorFlow 2.16.1 on a tensorflow:2.16.1-gpu devcontainer (CUDA 12.3, cuDNN 8.9). Note the dueling advantage mean is taken over ALL 141 actions including illegal ones, not over the legal set - a small but real bias. VRPO (RL/vrpo_model.py, azero-loop): two SEPARATE nets with the same trunk shape, deliberately not sharing a body. Actor 251->512->512->256->141 logits (author states 553k params); critic 251->[VRPO_
-- self_play: DQN main branch: pure mirror self-play with ONE network in both seats and one shared replay buffer; no opponent pool, no frozen snapshots, no seat handling beyond the negamax sign flip. The loser's terminal reward is injected retroactively: on a win, `self.model.memory[-1][2] += self.rewards.game(winner=False)` and `self.model.memory[-1][5] = True` (rl_game.py:112-113) - i.e. the opponent's last stored transition is reached back into and given -10 plus a done flag. Neat and worth copying, but it is why the two-ply target masks out the winner's penultimate transition (see pitfalls). azero-loop VRPO: `collect_vectorized` (RL/vrpo_game.py:92-201) runs 32 games in lockstep with one batched forwa
-- distributed: NONE. Single process, single GPU, TensorFlow 2.16.1 in a devcontainer with `--gpus all`. No tf.distribute, no MirroredStrategy, no multiprocessing, no Ray, no numba, no C++/Cython, no vectorized env. grep for multiprocessing/joblib/ray/numba/cython/torch/tf.distribute across all four branches returns only an unrelated `threading` import in play_vs_ai.py for the pygame window. The only speed engineering, all on the AI-written branches and all of the form "collapse many single-row Keras calls into one batched call": - Vectorized rollout: 32 games advanced in lockstep, one `act_batch` per step. Commit 982e038 claims ~8x faster collection; the docstring says rollout was 96% of wall-clock because
-- reported_strength: README (main): "Learns to play Splendor just above the upper level of skilled human performance"; "While the average BoardGameArena game length is 28 moves, this agent currently plays games averaging 25 moves"; footnote 3 then says "The reported 27-move average is on a sample size of 50 games played on BoardGameArena (48 wins and 2 losses)". EVIDENCE QUALITY: poor and internally inconsistent (25 vs 27 vs 28 moves in three places). The only artifact is reports/figures/avg_game_length.png, which I opened: it is a screenshot of BGA's AGGREGATE Splendor statistics across all players, all player counts, and including expansion content (rows for "Fullfilled cities" and "trading post"). Game length
-- reuse_for_us: DIRECTLY REUSABLE AS DESIGN (reimplement, do not copy - no license). 1. State encoding. The relative-cost trick (`max(cost - effective_gems, 0)/4` per color instead of raw cost) transfers verbatim to our 5 colors and our card set, and is probably the single highest-value idea for a small MLP. Our board is bigger though: our card templates are the same shape as base Splendor (40/30/20, +1 discount each) so an 11-dim card slot works, but our nobles are 3 pts and n+1 revealed, so the noble block must be sized 3-5 slots and made player-count-dependent rather than fixed at 3. Fix their bug while porting: nobles must be compared against CARDS ONLY, not effective_gems. 2. Missing features we must a
-- ideas_to_steal:
   * Relative-cost card encoding: encode every visible card as max(cost - effective_gems, 0)/4 plus a gem one-hot and points/15, so the net reads 'how far am I from buying this' rather than raw cost. Splendor/Environment/Splendor_components/Board_components/deck.py:24-27 (Card.to_vector) and :36-39 (Nobl
   * Shared-network negamax bootstrap: one net plays both seats out of one replay buffer, and the TD target simply negates the opponent-perspective next value - targets = rewards - (1-dones)*gamma*next_q. No seat feature, no duplicated network. Splendor/RL/model.py:272.
   * Two-ply 'own next state' target for alternating-mover games: sample index i and i+1 from the interleaved buffer so mem[i+1][3] is the acting player's own next state, then use a normal positive-gamma max target, masked by next2_valid = 1 - max(done_i, done_i+1). Generalizes to N players as 'skip N-1 
   * Retroactive terminal reward injection: when a player wins, reach back into the opponent's last stored transition to add the loss reward and set its done flag - memory[-1][2] += loss_reward; memory[-1][5] = True. Splendor/Environment/rl_game.py:112-113.
   * Factor combinatorial sub-choices out of the action space and delegate them to a heuristic aligned with what the net wanted: enumerate (take-combo x number of discards) rather than (take-combo x discard multiset), and pick WHICH gems to drop by preferring colors not just taken. Splendor/Environment/S
   * Binary 'with gold / without gold' buy actions instead of per-color gold allocation, with legality reported as a pair (afford_wo_gold, afford_with_gold) and a gold_choice_exists predicate to detect when the two genuinely differ. player.py:195-221 and :223-245.
   * Shape unforced-but-legal weak moves instead of masking them: taking fewer than 3 gems when 3 were available costs -0.6, keeping the action legal so the net can still use it when it is actually right. Splendor/RL/rewards.py:297-320 (BasicRewardEngine.GemRewards).
   * Potential-style card valuation: a turns-to-afford lower bound (ceil(colored_need/3) + forced_gold + 1 for the buy turn) used as the denominator of a points-plus-discount-value numerator, so cards are scored as value per turn. Splendor/RL/rewards.py:96-119 (turn_efficiency) and :165-175 (turns).
   * Opportunity-cost / blocking reward: rank the OPPONENT's top-2 reachable cards, and reward taking or reserving their number-one only by the gap between their best and second-best option, damped by 0.88 for the chance the replacement is also good. Splendor/RL/rewards.py:329-359 (BuyRewards.opportunity
   * Shaping-to-sparse anneal via a wrapper: BlendedRewardEngine scales every shaped component by a live alpha read off the agent each move while keeping the +/-10 win/loss at full strength, so alpha=1 is dense and alpha=0 is pure sparse, annealed over a [0.2, 0.8] window of the run. RL/rewards.py:452-50
   * PUCT search over the real engine with per-simulation determinization: snapshot/restore only mutable containers (Card and Noble objects are immutable so refs are shared), reshuffle the decks every simulation so search never peeks at future draws, and key the tree by ACTION PATH (open loop) with selec
   * Batched MCTS leaf evaluation with virtual loss: run eval_batch descents applying (N+1, W-1) along each path so in-batch siblings diversify, evaluate all pending leaves in one network call, then undo the virtual loss exactly and apply the real backup; eval_batch=1 reproduces the sequential algorithm 
-- pitfalls:
   * main branch does not import. RL/__init__.py line 6 does `from .rewards import RewardEngine` and rewards.py defines no RewardEngine (only BasicRewardEngine / SparseRewardEngine / SparseReserveRewardEngine). Environment/rl_game.py:6 and gui_game.py:8 import it too. Only the AI-written branches add the
   * README claims 'Double-DQN with PER' but there is no prioritized replay anywhere. model.py:398 is `sample(self.memory, self.batch_size)` - uniform sampling from a deque. grep for priorit/sumtree/importance-sampling returns nothing.
   * The strength claim is not supported by anything reproducible: no trained weights in the repo (trained_agents/ is gitignored), no evaluation harness on main, no opponent baselines, no logs. The single artifact (reports/figures/avg_game_length.png) is a BGA population-average screenshot spanning all s
   * The repo's own branches show the DQN champion is far from strong: 150-sim PUCT with the same nets beats it 29-1 (mcts.py docstring), and every training-time lever plateaued around 0.6 win rate. Treat 'superhuman' as marketing.
   * The checked-in configuration is not the configuration that produced the claimed model. rl_game.py:14 hard-codes `SparseRewardEngine` (rewards are 0 except +/-10), while README footnote 4 says the results came from 'extremely hard-coded rewards in the RewardEngine' and gui_game.py uses BasicRewardEng
   * replay_two_ply masks out the WINNER's penultimate transition. The winner's move sets the loser's already-stored transition done=True, so when i is the winner's second-to-last transition, next2_dones is 1 and next2_valid becomes 0 - the bootstrap is dropped and, under SparseRewardEngine, the target b
   * Deque indexing is O(n) in Python. Both `sample(deque_of_50k, 512)` and `mem[i]`/`mem[i+1]` for 512 random indices walk the deque; at 50k entries that is millions of pointer hops per gradient step. Use a preallocated numpy ring buffer.
   * Off-by-one in every TensorBoard action map. `_get_action_indices` places buys at 95-124 and reserves at 125-139, but apply_move places takes at 0-95, buys at 96-125, reserves at 126-140. The buyIdx_to_pointIdx offsets for reserved-card buys are also off by one (157+12+i*11+5 instead of 157+13+...). 
